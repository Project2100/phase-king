To run the project, open a terminal in this folder, then run:

    docker compose build
    docker compose up

Some parameters can be comfortably tuned in the file `.env`.

In order to compile the report document from source, the package `nonotation` is required, which can be found [here](https://github.com/Project2100/math-nonotation).
